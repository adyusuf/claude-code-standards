# This repository carries ONLY the hand-written method layer.
# Everything below is Claude Code runtime data and NEVER enters it.
projects/
sessions/
session-env/
shell-snapshots/
telemetry/
cache/
tasks/
backups/
plans/
file-history/
scheduled-tasks/
plugins/
history.jsonl
.claude.json*
.credentials.json
policy-limits.json
remote-settings.json
mcp-needs-auth-cache.json
settings.local.json
*.pem
*.key
.DS_Store

# Symlink to the live configuration (local; never committed)
config

# Python bytecode is generated, never committed
__pycache__/
*.pyc

# Generated and rewritten by a live hook; it stays local (see docs/README.md)
docs/measurement-ledger.tsv
# Real project keys and the per-project cost report: local only (see docs/README.md)
docs/project-nicknames.tsv
docs/measurement-daily-by-project.md
docs/measurement-daily-by-role.md
docs/measurement-daily-by-kind.md
docs/measurement-daily-by-model.md
.codeql/
